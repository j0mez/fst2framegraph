-r requirements.txt
# Upstream frame-semantic-transformer 0.10.0 pins older inference dependencies
# that do not currently install cleanly on Python 3.12/macOS. Core graph,
# materialise, inspect, and convert workflows do not require this stack.
frame-semantic-transformer>=0.10.0; python_version < "3.12"
